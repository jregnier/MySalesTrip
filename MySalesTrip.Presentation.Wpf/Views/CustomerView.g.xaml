<UserControl x:Class="MySalesTrip.Presentation.Wpf.Views.CustomerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ns0="clr-namespace:AmmySidekick;assembly=AmmySidekick" ns0:Ammy.Register="/MySalesTrip.Presentation.Wpf;component/Views/CustomerView.g.xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="1*"/>
      <RowDefinition Height="50"/>
    </Grid.RowDefinitions>
    <Grid Row="0" MinWidth="200" Margin="50">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="1*"/>
        <ColumnDefinition Width="2*"/>
      </Grid.ColumnDefinitions>
      <Grid.Resources>
        <Style TargetType="{x:Type TextBlock}">
          <Setter Property="Margin" Value="0 0 0 50"/>
          <Setter Property="FontSize" Value="20"/>
        </Style>
      </Grid.Resources>
      <StackPanel Grid.Column="0" Orientation="Vertical">
        <TextBlock Text="Name"/>
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Vertical">
        <TextBox>
          <TextBox.Text>
            <Binding Path="Name"/>
          </TextBox.Text>
        </TextBox>
      </StackPanel>
    </Grid>
    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
      <Button Margin="0 0 5 0" Width="100">Cancel</Button>
      <Button Width="100">Save</Button>
    </StackPanel>
  </Grid>
</UserControl>